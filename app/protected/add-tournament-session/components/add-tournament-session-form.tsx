import { TournamentFormProps } from "@/lib/interfaces";

const TournamentForm: React.FC<TournamentFormProps> = ({
    userId,
    locations,
    game_types,
    currentSession
}) => { 
    //console.log(userId, locations, game_types, currentSession)
    return (
        <div>
            Hello
        </div>
    )
}

export default TournamentForm